
# 设计思路

## 项目整体架构设计

### setInterval 循环：

注意：这并不是同步循环，每轮循环里并不是这4件事情都要按顺序发生一遍。

1. 告警检测
2. agent（考虑到quickjs可能会阻塞主线程，如果真是这样就考虑把agent拆到一个worker里面）
3. 更新状态机
4. 推送 qq bot

## 假设前提：

1. 告警系统的规则数量少，且不存在 cpu 密集型的任务，不会大幅拖慢告警规则的定期触发。
2. 对告警规则的触发不存在秒级别甚至毫秒级的要求。

## 得到结论：

由上述两条可以得出，我们可以不用将计时器和告警检测拆分到两个 worker 里，也没有必要像 Prometheus 那样严格按照时间戳计算下次告警时间并统计错过的告警次数。

因此只需要一个 setInterval ，并借助 nodejs 的异步模型定期启动告警规则的检测即可。

## 告警

alertmanager 的核心 feature 包括：分组、抑制、静默、路由、去重

在本项目上作如下保留：

1. 分组：一个时间段内的同一接口的报错被分组到一起
2. 抑制：抑制关系靠规则检测的代码实现，比如规则产生信号a之后就跳过产生信号b的代码。暂时没想到我们的项目会存在比较复杂的抑制关系，因此暂时可以这么搞。
3. 静默：在特定时间段内无视特定告警，更倾向于手工配置（这个的优先级低一些，不着急搞）。

路由和去重目前感觉对我们这个级别的小项目意义不大。
