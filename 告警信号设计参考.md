# alert manager 设计

trigger ->

## `http_request_metrics_1m` 是什么？

这是一张**按 1 分钟窗口预聚合的 HTTP 接口指标表**。它不存原始请求日志，而是把每个接口在每个分钟内统计好一次，方便你快速看每个接口的请求量、错误量和耗时。

### 📋 表结构（7 列）

| 列名 | 类型 | 语义 | 说明 |
|------|------|------|------|
| `http.route` | String | **路由路径** | TAG 主键，如 `/hdjw/grade`、`/electricity`、`/ping` |
| `http.response.status_class` | String | **状态码类别** | TAG 主键，取值 `2xx` / `4xx` / `5xx` / `null` |
| `req_count` | Int64 | **请求总数** | 该分钟内该路由该类别的请求次数 |
| `error_count` | Int64 | **错误请求数** | 出错的请求次数 |
| `avg_duration_nano` | Float64 | **平均耗时（纳秒）** | 除以 1000000 就是毫秒 |
| `time_window` | TimestampNanosecond | **时间窗口** | 主键，1 分钟粒度，即这一行代表哪一分钟 |
| `update_at` | TimestampMillisecond | **表更新时间** | 该行最新写入时间 |

### 🔑 关键理解

1. **一行 = 一个路由 × 一个状态码类别 × 一分钟**。`http.route` 和 `http.response.status_class` 是 TAG（维度），`time_window` 是时间戳，三者共同唯一确定一行。

2. **看实际数据**（最近一分钟）：
   - `/electricity` 在 `2xx` 有 2 个请求、平均耗时 14ms；同时 `/electricity` 在 `5xx` 有 2 个请求、**全部失败**（error_count=2）——说明这个接口最近在报错。
   - `/hdjw/class-table` 平均耗时 1.27 秒，属于慢接口。

3. **这是聚合表，不是原始日志**。想看某一笔请求的具体报错原因，就得去查 `opentelemetry_traces`（原始 trace 表）。

### 📊 常见用法

```sql
-- 看某个接口的错误率
SELECT `http.route`,
       SUM(req_count) AS total,
       SUM(error_count) AS errors,
       SUM(error_count)/SUM(req_count)*100 AS err_rate
FROM http_request_metrics_1m
WHERE `http.route` = '/electricity'
GROUP BY `http.route`;
```

从第二组查询结果可以看到，`/ping`、`/info/smester`、`/jifen/total` 是最热门的接口，说明这些是访问量最高的功能。

## 1. `opentelemetry_traces` — 原始链路追踪（核心表）

**这是整个观测系统的核心**，后端每处理一个请求就会产生一条 trace，实际是分布式链路（trace）下的多个 span（跨度）。共 **101 列**，1377 万条。OpenTelemetry 标准格式。

### 结构分组

**① 时间与链路标识（6 列）**
| 列 | 类型 | 含义 |
|---|---|---|
| `timestamp` | ns | **主时间戳**，span 开始时间（TIMESTAMP 索引） |
| `timestamp_end` | ns | span 结束时间 |
| `duration_nano` | uint64 | 耗时（纳秒），`timestamp_end - timestamp` |
| `trace_id` | str | 一次完整请求的追踪 ID（一个请求的所有 span 共用） |
| `span_id` | str | 当前 span 的唯一 ID |
| `parent_span_id` | str | 父 span ID，用于串起调用链 |

**② span 属性（8 列）**
| 列 | 含义 |
|---|---|
| `span_name` | 操作名。SERVER 是 HTTP 路由（如 `/hdjw/grade`），INTERNAL/CLIENT 是函数名（如 `get_grade`） |
| `span_kind` | 类型：`SPAN_KIND_SERVER`（HTTP 请求）/ `SPAN_KIND_INTERNAL`（内部函数）/ `SPAN_KIND_CLIENT`（调上游/DB/微信） |
| `span_status_code` | 结果：`STATUS_CODE_ERROR`（错误）/ `STATUS_CODE_UNSET`（正常）。**注意没有 OK 值** |
| `span_status_message` | 错误信息，如"密码错误"、"请求超时" |
| `trace_state` / `scope_name` / `scope_version` | 元数据 |

**③ 标准资源/请求属性（约 20 列）**
请求方信息：`client.address`（客户端 IP）、`user_agent.original`（UA）、`http.request.method`、`url.path`、`url.query`、`http.route`、`http.response.status_code`、`status_class`、`subsystem`（业务子系统：hdjw/实验室/积分/一卡通等）、`stu_id`（学号）、`request_id`、`busy_ns`/`idle_ns`（忙/闲耗时拆分）、`panic`（是否崩溃）、`origin_type`。

**④ 业务属性（70 列）** — 按模块动态打点，很多是为特定功能加的：
- **成绩/教务**：`semester_id`、`course_id`、`jx0404id`、`is_graduate`、`year`、`month`、`day`、`week`、`jc`、`grade` 相关
- **爬取上游**（教务/网管）：`fetch_ms`、`parse_ms`、`tried_count`、`max_tried`、`tried`、`data_source`、`system`、`xn`、`xq`、`redirect_count` —— 这些是后端**抓上游系统的耗时和重试**，可以理解为"爬虫行为"的体现
- **缓存**：`cached`、`cache_result`、`redis_failed`、`token_hit`、`tag_hit`、`refresh`、`refresh_token`
- **异步队列**（`async_update_worker`）：`worker_id`、`originating_trace_id`、`try_push`、`already_in_queue`、`wait_time`、`exchange`、`routing_key`
- **宿舍/电量**：`dormitory`、`building_id`
- **积分**：`goods_id`、`outcome`、`lock_wait`、`lock_hold`
- **体测**：`history_type`、`using_stu_id`

**⑤ 事件/链路关联**
`span_events`（json）、`span_links`（json）

> **用途**：排查单次请求的链路时序、错误根因、耗时瓶颈、特定学号/路由的行为。是最细粒度、最贵的表。

---

## 2. `http_request_metrics_1m` — 1 分钟 HTTP 指标（预聚合表）

**只针对 HTTP 请求（SERVER span）做的 1 分钟粒度聚合**，共 7 列，查询最快。

| 列 | 类型 | 语义 |
|---|---|---|
| `http.route` | TAG | 路由路径（如 `/hdjw/grade`） |
| `http.response.status_class` | TAG | 状态类别：`2xx`/`4xx`/`5xx`/`null` |
| `req_count` | FIELD | 该路由+状态类在 1 分钟内的请求总数 |
| `error_count` | FIELD | 其中错误数 |
| `avg_duration_nano` | FIELD | 平均耗时（纳秒） |
| `time_window` | TIMESTAMP | 分钟窗口（1 分钟粒度） |
| `update_at` | FIELD | 表更新时间 |

> **用途**：**看 QPS、错误率、平均耗时**的流量大盘。按路由/状态类/时间聚合最方便，性能最好。比如"`/hdjw/grade` 最近 1 小时 5xx 有多少、平均多慢"。

---

## 3. `opentelemetry_traces_operations` — span 操作清单（物化视图）

**只存"有哪些 span_name + span_kind 的组合"**，4 列，用于快速枚举所有操作，避免在全量 trace 表上做 `SELECT DISTINCT`。

| 列 | 含义 |
|---|---|
| `timestamp` | 该组合最近出现的时间 |
| `service_name` | 服务名（当前只有 weihuda_backend） |
| `span_name` | 操作名 |
| `span_kind` | span 类型 |

> **用途**：发现全系统有哪些路由/函数，比如问你"系统有哪些接口"，直接查这张表。

---

## 4. `opentelemetry_traces_services` — 服务清单

就 2 列：`timestamp` + `service_name`。列出所有接入的服务名，**当前只有 1 个 `weihuda_backend`**。这就是为什么说"没有爬虫独立服务"。

---

## 5. `active_users_daily` — 每日活跃用户明细

| 列 | 含义 |
|---|---|
| `stu_id` | 学号（TAG） |
| `time_window` | 天窗口（TIMESTAMP） |
| `update_at` | 更新时间 |

每个 (学号, 天) 一行，表示该学号这天有活跃。**精准 DAU 明细**，可精确去重统计。

---

## 6. `active_users_daily_hll` — 日活 HLL 估算（轻量表）

| 列 | 含义 |
|---|---|
| `time_window` | 天窗口 |
| `state` | HyperLogLog 草图（varbinary 二进制） |
| `update_at` | 更新时间 |

用 HLL 算法压缩的日活估算，**不存明细学号**，查询用 `approx_count_distinct_sorted(state)` 估算 UV。**省内存、快**，但只能估算不能反查具体学号。

---

## 关系总结

```
用户请求进来
   ├─ 每个 span 落 opentelemetry_traces（最全，101 列）
   ├─ 同时按 1 分钟聚合到 http_request_metrics_1m（HTTP 大盘，快）
   ├─ 各 span 组合登记到 operations / services（供枚举）
每日结算
   ├─ active_users_daily（精确 DAU 明细）
   └─ active_users_daily_hll（HLL 估算 DAU）
```

### 实际使用建议
- **看流量/错误率/QPS** → `http_request_metrics_1m`
- **看单次请求链路、根因、耗时** → `opentelemetry_traces`
- **看有哪些接口/函数** → `opentelemetry_traces_operations`
- **看活跃用户数** → `active_users_daily`（精确）或 `active_users_daily_hll`（快速估算）
